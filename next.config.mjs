/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // allows unoptimized local images (useful for static export / GitHub Pages)
    unoptimized: true,
  },
};
export default nextConfig;
